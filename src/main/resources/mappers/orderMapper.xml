<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.HiBook.order.dao.OrderDAO">
 	<insert id="insertOrderByProductIdUserIdOrderproductIdCountPriceState" parameterType="map">
 		INSERT INTO `order`
 		(
 			`productId`
			,`userId`
			,`count`
			,`price`
			,`state`
			,`createdAt`
			,`updatedAt`
 		)
 		VALUES
 		(
 			#{productId}
 			, #{userId}
 			, #{count}
 			, #{price}
 			, DEFAULT
 			, NOW()
 			, NOW()
 			
 		)
 	</insert>
 </mapper>